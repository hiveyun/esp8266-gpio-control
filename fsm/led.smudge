led {
    * ready -(@initLed)-> off,

    off (@ledOff) [
        loop -(@ledLoop)-,
        toggle --> on,
        _ --,
    ],

    on (@ledOn) [
        loop -(@ledLoop)-,
        toggle --> off,
        _ --,
    ]
}
